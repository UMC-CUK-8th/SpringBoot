package umc.spring.repository.missionRepository;


import umc.spring.domain.Mission;
import umc.spring.web.dto.MissionCursor;

import java.time.LocalDate;
import java.util.List;
/*
public interface MissionRepositoryCustom {

    List<Mission> dynamicQueryWithBooleanBuilder(String missionSpec, Integer reward, LocalDate deadline);
    List<MissionCursor> dynamicQueryWithBooleanBuilder2(Long userId, String cursorValue, Long cursorId, int limit);
}
*/
